### 修复
- **macOS**：修复 Claude Desktop 会话被误识别为 CLI 的问题（跳转按钮错误显示 "Terminal"）
  - 扩展 transcript 路径匹配，覆盖新版 Claude Desktop 的 Application Support 路径
  - 当 peer 进程树检测失败时，增加 Claude Desktop 运行状态作为辅助判断信号
  - 避免在 Claude Desktop 正在运行时因前台应用为终端而错误判定为 CLI
