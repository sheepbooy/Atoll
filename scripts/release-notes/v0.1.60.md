### 修复
- **Agent hooks 安装**：table-driven 重构后 `atoll_events_json` 把每个事件写成单个 matcher 对象，upsert 因期望数组而整段跳过，安装写盘后校验失败并误报 `~/.claude/settings.json` 权限问题；已恢复为 matcher group 数组，Claude / Codex / ZCode / Gemini 一并修好
